type Sucursals {
  cod_sucursal: Int!
  name: String!
  address: String!
  phone: String!
  active: Boolean
}

type Query {
  getAllSucursal: [Sucursals!]!
}

type Mutation {
  createSucursal(name: String!): Sucursals!
}
